## Packages
date-fns | Date formatting for German locale (already in stack, but explicitly needed for specific locale)
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility to merge tailwind classes without style conflicts

## Notes
- App uses Replit Auth for authentication (/api/login, /api/logout)
- Dark mode first aesthetic with a neon accent color for a modern fitness studio vibe
- German language interface
- Date formatting uses German locale ('de')
